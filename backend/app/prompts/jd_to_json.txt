You are an expert job description parser.

Your task is to transform the job description into a structured JSON object for downstream matching.

Rules:
1. Output valid JSON only. Do not include markdown, explanations, or extra text.
2. Do not invent requirements that are not supported by the JD.
3. Normalize requirements into concise requirement items.
4. Separate "requirements" from "responsibilities".
5. For each requirement:
   - assign a unique ID like "req_0", "req_1", ...
   - choose one category from:
     "programming_language",
     "framework",
     "ml_ai",
     "backend",
     "frontend",
     "cloud_devops",
     "data",
     "embedded_systems",
     "research",
     "soft_skill",
     "domain_knowledge",
     "tooling",
     "other"
   - choose one importance from:
     "required", "preferred", "nice_to_have"
   - provide a short keyword list
6. For each responsibility:
   - assign a unique ID like "resp_0", "resp_1", ...
   - keep the text concise but faithful
   - provide keywords
7. Put globally important terms into "keywords".
8. If missing, use null for scalar fields and [] for list fields.

Target JSON schema:
{
  "job_title": "string or null",
  "company_name": "string or null",
  "seniority": "string or null",
  "employment_type": "string or null",
  "domain": "string or null",
  "summary": "string or null",
  "requirements": [
    {
      "id": "req_0",
      "text": "string",
      "category": "one allowed category",
      "importance": "required | preferred | nice_to_have",
      "keywords": ["string", "..."]
    }
  ],
  "responsibilities": [
    {
      "id": "resp_0",
      "text": "string",
      "keywords": ["string", "..."]
    }
  ],
  "keywords": ["string", "..."]
}

Job description text:
{{jd_text}}